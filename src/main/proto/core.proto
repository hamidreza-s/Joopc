syntax = "proto3";
option java_multiple_files = true;
package com.wenower.core.proto;

message Packet {
    string payload = 1;
    string timestamp = 2;
}

service Core {
    rpc ping(Packet) returns (Packet);
}
